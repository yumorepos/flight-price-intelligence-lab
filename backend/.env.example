# Optional PostgreSQL URL (preferred for production)
FPI_DATABASE_URL=

# Optional CSV fallback mode for local/demo runtime
FPI_USE_CSV_FALLBACK=false

# Comma-separated CORS origins
FPI_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
